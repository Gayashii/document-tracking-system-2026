'use strict';

/** @param {import('knex').Knex} knex */
exports.seed = async function (_knex) {
  // Default workflow seed removed — workflow steps require specific assigned_user_id
  // values which are not known at seed time. Workflows are configured via the admin UI.
};
