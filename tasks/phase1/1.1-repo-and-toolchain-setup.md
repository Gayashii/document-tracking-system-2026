# Task 1.1 — Repository and Toolchain Setup

**Phase:** 1 — MVP
**Status:** [x] Complete

## Objective
Initialize the monorepo structure with shared tooling, Docker dev environment, and CI/CD pipelines.

## Steps

- [x] Initialize monorepo with two workspaces: `client/` (Angular) and `server/` (Express)
- [x] Configure ESLint + Prettier with a shared config at the root
- [x] Set up Husky pre-commit hooks (runs lint + unit tests before every commit)
- [x] Create `.env.example` in both `client/` and `server/` listing all required environment variables
- [ ] ~~Write `docker-compose.yml`~~ — out of scope, not needed
- [x] Add `server/uploads/` to `.gitignore`
- [ ] ~~Create `.github/workflows/ci.yml`~~ — out of scope, not needed
- [ ] ~~Create `.github/workflows/deploy.yml`~~ — out of scope, not needed
- [x] Add root `.gitignore` (node_modules, .env, build artifacts, coverage)
- [x] Write `README.md` with local setup instructions

## Acceptance Criteria
- Pre-commit hook blocks commits that fail lint or tests

## Dependencies
None — this is the first task.
