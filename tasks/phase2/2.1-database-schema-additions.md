# Task 2.1 — Full Database Schema Additions

**Phase:** 2 — Core Features
**Status:** [x] Complete

## Objective
Add all remaining tables required by the workflow engine, notifications, and document versioning.

## Steps

- [x] Create migration: `workflows` table (`20260402000001_create_workflows.js`)
  - id, name, document_type_id (FK, nullable — null = default workflow), created_at
- [x] Create migration: `workflow_steps` table (`20260402000002_create_workflow_steps.js`)
  - id, workflow_id (FK), step_order INT, required_role (reuses `user_role` ENUM), action_label VARCHAR, is_final BOOL, created_at
  - unique constraint on (workflow_id, step_order)
- [x] Create migration: `notifications` table (`20260402000003_create_notifications.js`)
  - id, recipient_id (FK → users), type ENUM('email','sms','in_app'), subject, body TEXT, status ENUM('pending','sent','failed'), sent_at, created_at
- [x] Create migration: `document_versions` table (`20260402000004_create_document_versions.js`)
  - id, document_id (FK), version_number INT, file_path, uploaded_by (FK → users), created_at, change_note
- [x] Create migration: `system_settings` table (`20260402000005_create_system_settings.js`)
  - id, key (unique), value TEXT, updated_by (FK → users), updated_at
- [x] Add index: `notifications(recipient_id, status)` for fast unread-count queries
- [x] Add index: `document_versions(document_id, version_number)`
- [x] Seed default workflow: Student Submits → Finance Staff Verifies → Head of Finance Approves → Payment Processed (`seeds/04_default_workflow.js`)

## Acceptance Criteria
- `knex migrate:latest` succeeds with no errors
- All FK constraints are enforced
- Default workflow seed is present and linked to all document types

## Dependencies
- Task 1.2 (base schema must exist)
