# Task 3.2 — Reporting and Export

**Phase:** 3 — Advanced Features
**Status:** [x] Complete

## Objective
Generate server-side reports with CSV, Excel, and PDF export, scoped by the requesting user's role.

## Steps

### Dependencies
- [x] Install: `fast-csv`, `exceljs`, `pdfkit`

### Report Service (`services/reportService.js`)
- [x] `getPendingReport(filters)`: documents in `submitted` and `pending_approval` status, filterable by date range and department. Includes: reference number, title, student name, submission date, days pending.
- [x] `getHistoryReport(filters)`: full document lifecycle — all status transitions per document. Includes: reference number, title, each status with timestamp, actor, note.
- [x] `getStatisticsReport(filters)`: aggregated counts grouped by document type, status, department, academic year. Includes: totals, averages, trend data.
- [x] `getOverdueReport(filters)`: documents in `pending_approval` for more than N days (configurable in `system_settings`).
- [x] All report methods respect role scoping (finance staff sees only their department if `department_id` is set on their user record).

### Export Service (`services/exportService.js`)
- [x] `toCSV(rows, columns)`: stream CSV to HTTP response using `fast-csv`; no temp file created
- [x] `toExcel(rows, columns, sheetName)`: build `exceljs` workbook in memory, pipe to response; styled headers (bold, background color)
- [x] `toPDF(rows, columns, title)`: build `pdfkit` document in memory, pipe to response; includes report title, generation timestamp, and page numbers

### Controllers & Routes (`report.routes.js`)
- [x] `GET /api/v1/reports/pending` — roles: finance_staff, admin, auditor
- [x] `GET /api/v1/reports/history` — roles: admin, auditor
- [x] `GET /api/v1/reports/statistics` — roles: admin, auditor, finance_staff
- [x] `GET /api/v1/reports/overdue` — roles: finance_staff, admin, auditor
- [x] `GET /api/v1/reports/export?report=pending|history|statistics&format=csv|xlsx|pdf` — roles: admin, auditor
- [x] Rate limit export endpoints: 5 req/min/IP

### Frontend (`src/app/features/reports/ReportsDashboardComponent`)
- [x] Summary cards: Pending Documents count, Approved Today count, Rejected This Week count, Overdue count
- [x] Report tables with filters (date range, department, document type)
- [x] Export buttons: "Download CSV", "Download Excel", "Download PDF" — trigger file download via `<a>` with `download` attribute pointing to export endpoint
- [x] Charts: simple bar chart using native SVG (no charting library) showing documents by status

### Tests
- [x] Unit: CSV export streams correct number of rows with correct headers
- [x] Unit: PDF export includes title and timestamp
- [x] Integration: call pending report, verify only pending documents appear; verify finance_staff scope works
- [x] Integration: export as CSV, parse result, verify row count matches report query

## Acceptance Criteria
- Reports are generated server-side (never client-side)
- Exports stream to the browser without creating temp files on disk
- Finance staff can only export data within their role scope
- PDF report includes system name, generation date, and page numbers
- Export endpoints return appropriate `Content-Disposition: attachment` headers

## Dependencies
- Task 2.5 (RBAC), Task 1.5 (documents), Task 2.2 (status history), Task 2.1 (system_settings table)
